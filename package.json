{
  "dependencies": {
    "glob": "^7.1.2",
    "mustache": "^2.3.0",
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "eslint": "^4.19.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.9.0"
  },
  "scripts": {
    "test": "ava src/*.test.js",
    "build:strategy": "node src/build.js strategy",
    "build:alerts": "node src/build.js study",
    "build": "npm run build:strategy | grep -v '^>' | pbcopy"
  }
}
